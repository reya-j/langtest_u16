language: node_js
build:
  ci:
    - node --version
    - docker info
    - echo Link to https://rcapp.shippable.com
    #- sleep 5m
    
  pre_ci_boot:
    image_name: drydock/u16 
    image_tag: master
    pull: true 
